<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="56dp"
    android:layout_width="match_parent">

    <item android:id="@+id/termsNavAction"
        app:showAsAction="always"
        android:title="Terms"
        android:icon="@drawable/ic_action_term"/>
    <item android:id="@+id/coursesNavAction"
        app:showAsAction="always"
        android:title="Courses"
        android:icon="@drawable/ic_action_course"/>
    <item android:id="@+id/assessmentsNavAction"
        app:showAsAction="always"
        android:title="Assessments"
        android:icon="@drawable/ic_action_assessment"/>

</menu>